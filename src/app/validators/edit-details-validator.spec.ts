import { EditDetailsValidator } from './edit-details-validator';

describe('EditDetailsValidator', () => {
  it('should create an instance', () => {
    expect(new EditDetailsValidator()).toBeTruthy();
  });
});
